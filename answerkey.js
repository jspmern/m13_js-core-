//1.  T
let str=""
    for(let i=1;i<=5;i++ )
        {
                 for(let j=1;j<=5;j++)
                 {
                    if(i==1 || j==3)
                    {
                        str= str+"*"
                    }
                    else{
                        str=str+" "
                    }
                 }
                 str=str+'\n'
        }
        console.log(str)
//******************************************************** */

//2. H
  let str=""
    for(let i=1;i<=5;i++ )
        {
                 for(let j=1;j<=5;j++)
                 {
                    if(j==1 || j==5 ||i==3 )
                    {
                        str= str+"*"
                    }
                    else{
                        str=str+" "
                    }
                 }
                 str=str+'\n'
        }
        console.log(str)
        //******************************************************** */

// 3.(output guess)
// a.6
// b. 5
// c.NaN
// d.0
// e. 2
//******************************************************** */

